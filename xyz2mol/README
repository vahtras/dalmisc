xyz2mol
-------

This is a simple script to convert molecular coordinates to a Dalton input file (the mol -file).

Installation:
    $ tar xvfz xyz2mol-1.0.tar.gz
    $ cd  xyz2mol-1.0/
    $ python setup.py check
    $ python setup.py install --user
